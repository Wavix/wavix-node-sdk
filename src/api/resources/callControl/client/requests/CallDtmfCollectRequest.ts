// This file was auto-generated by Fern from our API Definition.

import type * as Wavix from "../../../../index.js";

/**
 * @example
 *     {
 *         id: "id"
 *     }
 */
export interface CallDtmfCollectRequest {
    /** The `uuid` of the call. */
    id: string;
    /** Maximum number of digits to collect. */
    max_digits?: number;
    /** Timeout for digit collection in seconds. */
    timeout?: number;
    /** DTMF character that ends input collection. */
    termination_character?: string;
    /** Maximum number of attempts. */
    max_attempts?: number;
    /**
     * Prompt to play before collecting digits.
     *  Play a prerecorded audio file or use Wavix Text-To-Speech.
     */
    prompt?: CallDtmfCollectRequest.Prompt;
}

export namespace CallDtmfCollectRequest {
    /**
     * Prompt to play before collecting digits.
     *  Play a prerecorded audio file or use Wavix Text-To-Speech.
     */
    export interface Prompt {
        /** Audio file URL. */
        play?: string | undefined;
        /** Text to speak and voice to use. Pick `voice` from the language family matching `language` (for example, a German voice such as `Hans` for `ge`, a Spanish voice such as `Conchita` for `sp`) — Wavix does not validate the pairing itself. */
        say?: Prompt.Say | undefined;
    }

    export namespace Prompt {
        /**
         * Text to speak and voice to use. Pick `voice` from the language family matching `language` (for example, a German voice such as `Hans` for `ge`, a Spanish voice such as `Conchita` for `sp`) — Wavix does not validate the pairing itself.
         */
        export interface Say {
            /** Text to speak. */
            text: string;
            language?: Wavix.TtsLanguage | undefined;
            voice: Wavix.TtsVoiceId;
        }
    }
}
