// This file was auto-generated by Fern from our API Definition.

import type * as Wavix from "../../../../../../index.js";

/**
 * @example
 *     {
 *         sender_id: "Wavix",
 *         type: "numeric",
 *         countries: ["countries"],
 *         usecase: "transactional"
 *     }
 */
export interface SenderIdCreateRequest {
    /** Sender ID name. Can be either an alphanumeric string or a  phone number. */
    sender_id: string;
    type: Wavix.SenderIdType;
    /** Two-letter ISO country codes where the Sender ID is allowlisted. */
    countries: string[];
    /** Primary use case for the Sender ID. One of `transactional` (account or order notifications), `promo` (marketing and promotional messages), or `authentication` (one-time passcodes and verification codes). */
    usecase: SenderIdCreateRequest.Usecase;
    /** Expected number of messages sent per month from the Sender ID. One of `1-1000`, `1001-20000`, `20001-50000`, `50001-100000`, or `More than 100000`. Each value is the message-count band for the month. */
    monthly_volume?: SenderIdCreateRequest.MonthlyVolume;
    /** Message samples. */
    samples?: string[];
}

export namespace SenderIdCreateRequest {
    /** Primary use case for the Sender ID. One of `transactional` (account or order notifications), `promo` (marketing and promotional messages), or `authentication` (one-time passcodes and verification codes). */
    export const Usecase = {
        Transactional: "transactional",
        Promo: "promo",
        Authentication: "authentication",
    } as const;
    export type Usecase = (typeof Usecase)[keyof typeof Usecase];
    /** Expected number of messages sent per month from the Sender ID. One of `1-1000`, `1001-20000`, `20001-50000`, `50001-100000`, or `More than 100000`. Each value is the message-count band for the month. */
    export const MonthlyVolume = {
        One1000: "1-1000",
        OneThousandOne20000: "1001-20000",
        Undefined50000: "20001-50000",
        Undefined100000: "50001-100000",
        MoreThan100000: "More than 100000",
    } as const;
    export type MonthlyVolume = (typeof MonthlyVolume)[keyof typeof MonthlyVolume];
}
