// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         session_id: "2953d4308f2e11ecb75fcdafd6d2d687",
 *         channel: "sms"
 *     }
 */
export interface TwoFactorVerificationResendRequest {
    /** The unique ID of the 2FA verification session. */
    session_id: string;
    /** Channel used to resend the verification code. One of `sms` (sent as a text message) or `voice` (read aloud over a phone call). */
    channel: TwoFactorVerificationResendRequest.Channel;
}

export namespace TwoFactorVerificationResendRequest {
    /** Channel used to resend the verification code. One of `sms` (sent as a text message) or `voice` (read aloud over a phone call). */
    export const Channel = {
        Sms: "sms",
        Voice: "voice",
    } as const;
    export type Channel = (typeof Channel)[keyof typeof Channel];
}
