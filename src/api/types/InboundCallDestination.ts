// This file was auto-generated by Fern from our API Definition.

import type * as Wavix from "../index.js";

/**
 * Inbound call destination
 */
export interface InboundCallDestination {
    /** Unique identifier of the inbound call destination */
    id: number;
    /** The destination for inbound call routing. Can be either a SIP URI, a PSTN phone number or system-generated login of a SIP trunk on the platform. */
    destination: string;
    /** For phone numbers with several destinations, sets the order in which the platform routes inbound calls. The lower the value, the higher the priority. */
    priority: number;
    transport: Wavix.InboundCallTransport;
    /** Unique identified of a SIP trunk on the platform. In cases when `transport:5`, otherwise, `null` */
    trunk_id: number | null;
    /** Indicates whether SRTP media encryption is enabled for this destination. */
    srtp?: boolean | undefined;
    /** A user-defined name of a SIP trunk on the platform. In cases when `transport:5`, otherwise, `null` */
    trunk_label: string | null;
}
