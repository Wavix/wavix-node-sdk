// This file was auto-generated by Fern from our API Definition.

export interface ShortLinkMetricsItem {
    /**
     * Latitude of the location derived from the IP address
     *  used to open the short link.
     */
    latitude: number | null;
    /**
     * Longitude of the location derived from the IP address
     *  used to open the short link.
     */
    longitude: number | null;
    /** Operating system of the device that opened the short link. */
    operating_system: string | null;
    /** Browser used to open the short link. */
    browser: string | null;
    /** Browser language preference. */
    language: string | null;
    /** Phone number associated with the short link. */
    phone: string | null;
    /** UTM campaign name associated with the short link. */
    utm_campaign: string | null;
    /** Date and time when short link was opened. */
    created_at: string;
    /** Hash of the short link. */
    link_hash: string;
    /** Account ID. */
    user_id: number;
}
