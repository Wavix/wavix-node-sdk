// This file was auto-generated by Fern from our API Definition.

/**
 * Sub-account details including API key and webhook configurations.
 */
export interface SubOrganizationResponse {
    /** Sub-account ID. */
    id: number;
    /** Date and time the sub-account was created in ISO 8601 format. */
    created_at: string;
    /** Sub-account name. */
    name: string;
    /** Sub-account API key. */
    api_key: string;
    /** Master account ID. */
    master_organization: number;
    /** Status of the subaccount. One of `enabled` (the subaccount is active and can be used) or `disabled` (the subaccount is suspended). */
    status: SubOrganizationResponse.Status;
    /** Default webhook URLs for inbound messages and delivery reports. */
    default_destinations: SubOrganizationResponse.DefaultDestinations;
}

export namespace SubOrganizationResponse {
    /** Status of the subaccount. One of `enabled` (the subaccount is active and can be used) or `disabled` (the subaccount is suspended). */
    export const Status = {
        Enabled: "enabled",
        Disabled: "disabled",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];

    /**
     * Default webhook URLs for inbound messages and delivery reports.
     */
    export interface DefaultDestinations {
        /** Inbound messages webhook URL. */
        sms_endpoint: string;
        /** Delivery report webhook URL. */
        dlr_endpoint: string;
    }
}
