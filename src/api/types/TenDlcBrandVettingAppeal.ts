// This file was auto-generated by Fern from our API Definition.

/**
 * Represents an appeal against a 10DLC brand vetting result. An appeal asks the vetting provider to reconsider the assigned score.
 */
export interface TenDlcBrandVettingAppeal {
    /** Outcome of the appeal, including the revised vetting status and score. */
    appeal_outcome: TenDlcBrandVettingAppeal.AppealOutcome;
    /** Current status of the appeal, such as `PENDING` or `COMPLETE`. */
    appeal_status: string;
    /** Timestamp when the appeal status was last updated, in ISO 8601 format. */
    appeal_status_update_date: string;
    /** UUIDs of the evidence files submitted in support of the appeal. */
    attachment_uuid_list: string[];
    /** Unique identifier of the Brand the appeal is associated with. */
    brand_id: string;
    /** Categories that classify the appeal, such as `LOW_SCORE`. */
    category_list: string[];
    /** Timestamp when the appeal was created, in ISO 8601 format. */
    create_date: string;
    /** Justification provided for the appeal. */
    explanation: string;
    /** Code identifying the external vetting provider that handled the appeal. */
    evp_id: string;
    /** Class of the vetting being appealed, such as `STANDARD` or `ENHANCED`. */
    vetting_class: string;
    /** Unique identifier of the vetting request being appealed. */
    vetting_id: string;
}

export namespace TenDlcBrandVettingAppeal {
    /**
     * Outcome of the appeal, including the revised vetting status and score.
     */
    export interface AppealOutcome {
        /** Current status of the Brand vetting */
        vet_status: string;
        /** The Brand vetting score */
        vet_score: number;
        /** The feedback provided by the external vetting provider */
        feedback: AppealOutcome.Feedback;
    }

    export namespace AppealOutcome {
        /**
         * The feedback provided by the external vetting provider
         */
        export interface Feedback {
            /** An list of human-readable explanations returned by TCR */
            reasons: string[];
        }
    }
}
