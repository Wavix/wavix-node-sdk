// This file was auto-generated by Fern from our API Definition.

/**
 * Represents per-MNO 10DLC requirements and throughput limits that apply to a campaign use case.
 */
export interface TenDlcmnoMetadata {
    /** MMS throughput per minute allowed on the AT&T network. */
    att_mms_tpm: number | null;
    /** Message class assigned by AT&T. */
    att_msg_class: string | null;
    /** SMS throughput per minute allowed on the AT&T network. */
    att_sms_tpm: number | null;
    /** Scope of the AT&T throughput-per-minute allocation. */
    att_tpm_scope: string | null;
    /** Indicates whether help keywords and an acknowledgement are mandatory for the use case. */
    help_required: boolean;
    /** Indicates whether an opt-in mechanism is mandatory for the use case. */
    optin_required: boolean;
    /** Indicates whether an opt-out mechanism is mandatory for the use case. */
    optout_required: boolean;
    /** Minimum number of message samples the MNO requires for the use case. */
    min_msg_samples: number;
    /** Name of the mobile network operator (MNO) these requirements apply to. */
    mno: string;
    /** Indicates whether the Brand qualifies with the MNO for the selected use case. */
    mno_qualify: boolean;
    /** Indicates whether the MNO requires a post-approval review of the use case. */
    mno_review: boolean;
    /** Indicates whether the MNO supports the use case. */
    mno_support: boolean;
    /** Indicates whether embedded links are prohibited in message content for the use case. */
    no_embedded_links: boolean;
    /** Indicates whether embedded phone numbers are prohibited in message content for the use case. */
    no_embedded_phone: boolean;
    /** Daily message cap allowed on the T-Mobile network. */
    tmobile_brand_dcap: number | null;
    /** Brand tier assigned on the T-Mobile network, such as `LOW`. */
    tmobile_brand_tier: string | null;
}
