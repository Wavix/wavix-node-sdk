// This file was auto-generated by Fern from our API Definition.

/**
 * Represents a voice campaign that places an outbound call running a pre-approved call flow.
 */
export interface VoiceCampaignResponse {
    /** Unique identifier of the call flow to launch, listed on the Call flows page. Every scenario must be pre-approved by the Wavix Service Operations team before it can be used in production. */
    callflow_id: number;
    /** Phone number on the account used as the Caller ID for the outbound call, in E.164 format. */
    caller_id: string;
    /** Destination phone number the outbound call is placed to, in E.164 format. */
    contact: string;
    /** Webhook URL that receives voice campaign status updates. */
    callback_url?: string | undefined;
}
