// This file was auto-generated by Fern from our API Definition.

/**
 * RFC 9457 problem document returned by WhatsApp endpoints as `application/problem+json`.
 */
export interface WhatsAppProblem {
    /** URI identifying the problem type. */
    type: string;
    /** Short, human-readable summary of the problem type. */
    title: string;
    /** HTTP status code. */
    status: number;
    /** Human-readable explanation specific to this occurrence of the problem. */
    detail: string;
    /** URI identifying this specific occurrence of the problem, such as the request ID. */
    instance: string;
    /** Machine-readable classification of the error. */
    error_class?: WhatsAppProblem.ErrorClass | undefined;
    /** Stable machine-readable error code, derived from the problem slug. */
    error_code?: string | undefined;
}

export namespace WhatsAppProblem {
    /** Machine-readable classification of the error. */
    export const ErrorClass = {
        Permanent: "permanent",
        Retriable: "retriable",
        RateLimited: "rate_limited",
        InvalidTemplate: "invalid_template",
    } as const;
    export type ErrorClass = (typeof ErrorClass)[keyof typeof ErrorClass];
}
