// This file was auto-generated by Fern from our API Definition.

export interface WhatsAppSender {
    /** Sender's phone number in E.164 format. */
    phone_number: string;
    /** Sender's WhatsApp display name. */
    display_name?: (string | null) | undefined;
    /** Sender registration status with the provider. */
    status: WhatsAppSender.Status;
    /** Sender's messaging quality rating. */
    quality_rating?: (WhatsAppSender.QualityRating | null) | undefined;
    /** Sender's 24-hour outbound messaging limit tier. */
    message_tier?: (WhatsAppSender.MessageTier | null) | undefined;
}

export namespace WhatsAppSender {
    /** Sender registration status with the provider. */
    export const Status = {
        Banned: "banned",
        Connected: "connected",
        Deleted: "deleted",
        Disconnected: "disconnected",
        Flagged: "flagged",
        Migrated: "migrated",
        Pending: "pending",
        RateLimited: "rate_limited",
        Restricted: "restricted",
        Unknown: "unknown",
        Unverified: "unverified",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    /** Sender's messaging quality rating. */
    export const QualityRating = {
        High: "high",
        Medium: "medium",
        Low: "low",
        Unknown: "unknown",
    } as const;
    export type QualityRating = (typeof QualityRating)[keyof typeof QualityRating];
    /** Sender's 24-hour outbound messaging limit tier. */
    export const MessageTier = {
        LimitNa: "limit_na",
        Limit250: "limit_250",
        Limit2K: "limit_2k",
        Limit10K: "limit_10k",
        Limit100K: "limit_100k",
        Unlimited: "unlimited",
    } as const;
    export type MessageTier = (typeof MessageTier)[keyof typeof MessageTier];
}
