// This file was auto-generated by Fern from our API Definition.

import * as Wavix from "../../../src/api/index";
import { WavixClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("StreamsClient", () => {
    test("create (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            stream_url: "wss://examples.com/stream",
            stream_type: "oneway",
            stream_channel: "inbound",
        };
        const rawResponseBody = { success: true, stream_id: "123e4567-e89b-12d3-a456-426614174000" };

        server
            .mockEndpoint()
            .post("/v1/calls/call_id/streams")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.callControl.streams.create({
            call_id: "call_id",
            stream_url: "wss://examples.com/stream",
            stream_type: "oneway",
            stream_channel: "inbound",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("create (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { stream_url: "stream_url", stream_type: "oneway", stream_channel: "inbound" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/calls/call_id/streams")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.callControl.streams.create({
                call_id: "call_id",
                stream_url: "stream_url",
                stream_type: "oneway",
                stream_channel: "inbound",
            });
        }).rejects.toThrow(Wavix.BadRequestError);
    });

    test("create (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { stream_url: "stream_url", stream_type: "oneway", stream_channel: "inbound" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/v1/calls/call_id/streams")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.callControl.streams.create({
                call_id: "call_id",
                stream_url: "stream_url",
                stream_type: "oneway",
                stream_channel: "inbound",
            });
        }).rejects.toThrow(Wavix.UnauthorizedError);
    });

    test("delete (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { success: true };

        server
            .mockEndpoint()
            .delete("/v1/calls/call_id/streams/id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.callControl.streams.delete({
            call_id: "call_id",
            id: "id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("delete (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .delete("/v1/calls/call_id/streams/id")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.callControl.streams.delete({
                call_id: "call_id",
                id: "id",
            });
        }).rejects.toThrow(Wavix.UnauthorizedError);
    });
});
