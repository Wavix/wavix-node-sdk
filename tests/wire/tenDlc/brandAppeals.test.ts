// This file was auto-generated by Fern from our API Definition.

import * as Wavix from "../../../src/api/index";
import { WavixClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("BrandAppealsClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = [
            {
                categories: ["VERIFY_TAX_ID"],
                created_at: "2024-08-01T14:09:43Z",
                evidence: ["13d8e00c-3cb4-4dc0-9e26-d5057fa938d9"],
                outcome: {
                    optional_attributes: { key: "value" },
                    vetting_status: "VERIFIED",
                    feedback: {
                        category: [
                            {
                                id: "VERIFY_TAX_ID",
                                display_name: "Verify tax ID",
                                description:
                                    "Select this category if the record is UNVERIFIED due to an inability to match the tax ID.",
                                fields: null,
                                errors: [{ code: "TFTI01", message: "The submitted US EIN is invalid." }],
                            },
                        ],
                    },
                },
                status: "COMPLETE",
                updated_at: "2024-08-01T18:33:15Z",
                explanation: "Dear partner, please review the registration docs",
            },
        ];

        server
            .mockEndpoint()
            .get("/v3/10dlc/brands/BM20QP9/appeals")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.tenDlc.brandAppeals.list({
            brand_id: "BM20QP9",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v3/10dlc/brands/brand_id/appeals")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.tenDlc.brandAppeals.list({
                brand_id: "brand_id",
            });
        }).rejects.toThrow(Wavix.BadRequestError);
    });

    test("list (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v3/10dlc/brands/brand_id/appeals")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.tenDlc.brandAppeals.list({
                brand_id: "brand_id",
            });
        }).rejects.toThrow(Wavix.ForbiddenError);
    });

    test("list (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v3/10dlc/brands/brand_id/appeals")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.tenDlc.brandAppeals.list({
                brand_id: "brand_id",
            });
        }).rejects.toThrow(Wavix.NotFoundError);
    });

    test("create (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            appeal_categories: ["VERIFY_TAX_ID"],
            evidence: ["855dff49-c097-4645-3983-08dcb9856232"],
        };
        const rawResponseBody = { success: true };

        server
            .mockEndpoint()
            .post("/v3/10dlc/brands/BM20QP9/appeals")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.tenDlc.brandAppeals.create({
            brand_id: "BM20QP9",
            appeal_categories: ["VERIFY_TAX_ID"],
            evidence: ["855dff49-c097-4645-3983-08dcb9856232"],
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("create (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            appeal_categories: ["appeal_categories", "appeal_categories"],
            evidence: ["evidence", "evidence"],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v3/10dlc/brands/brand_id/appeals")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.tenDlc.brandAppeals.create({
                brand_id: "brand_id",
                appeal_categories: ["appeal_categories", "appeal_categories"],
                evidence: ["evidence", "evidence"],
            });
        }).rejects.toThrow(Wavix.BadRequestError);
    });

    test("create (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            appeal_categories: ["appeal_categories", "appeal_categories"],
            evidence: ["evidence", "evidence"],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v3/10dlc/brands/brand_id/appeals")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.tenDlc.brandAppeals.create({
                brand_id: "brand_id",
                appeal_categories: ["appeal_categories", "appeal_categories"],
                evidence: ["evidence", "evidence"],
            });
        }).rejects.toThrow(Wavix.NotFoundError);
    });

    test("create (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new WavixClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            appeal_categories: ["appeal_categories", "appeal_categories"],
            evidence: ["evidence", "evidence"],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v3/10dlc/brands/brand_id/appeals")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.tenDlc.brandAppeals.create({
                brand_id: "brand_id",
                appeal_categories: ["appeal_categories", "appeal_categories"],
                evidence: ["evidence", "evidence"],
            });
        }).rejects.toThrow(Wavix.UnprocessableEntityError);
    });
});
